services:
  couchdb:
    image: couchdb:latest
    ports:
      - "5984:5984"
    environment:
      COUCHDB_USER: "admin"
      COUCHDB_PASSWORD: "couchdb"
    volumes:
      - couchdata:/opt/couchdb/data
    restart: unless-stopped

volumes:
  couchdata:
